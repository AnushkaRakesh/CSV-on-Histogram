import React from 'react'

const image = () => {
    const url="https://placehold.it/500x500";
  return (
    <div>
      <img src="url"></img>
      <button onClick={next} onClick={this.React.onClick.bind(this)}>Next</button>
      <button onClick={this.React.onClick.bind(this)}
      const val= fetch(props)=>{
        [values]=>({
            values(this.props.children).map((values,index)=>{return values[index];})
        });
      }
    </div>
  )
}

export default image

